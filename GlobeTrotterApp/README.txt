GlobeTrotter App
================

This is a simplified placeholder version of the GlobeTrotter app.

How to Run:
1. Ensure Node.js is installed.
2. Run `npm install` in the root directory.
3. Run `npm run dev` to start the development server.

This package includes static files, frontend (React), and server setup.
